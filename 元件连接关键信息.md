*​ 去掉所有电阻、电容等无源器件后，**​只保留与程序相关的关键连接​**信息（含引脚编号、功能描述和必要的配置说明） ​*

## 一、主控芯片 ESP32‑S3（开发板 ESP32‑S3‑DevKitC‑1 N16R8）

### 1. I²S 音频输出（至 PCM5102 DAC）

```c
BCK  (位时钟)  → GPIO4
LRCK (左右时钟) → GPIO5
DIN  (数据)    → GPIO6
```

- PCM5102 工作在 **​硬件模式​**，无需 I²C 配置，只需 ESP32 端初始化 I²S 并连续发送 PCM 数据。

### 2. ADC 输入（电位器）

```c
VR1 滑动端 → GPIO1 (ADC1_CH0)
```

- 电位器两端分别接 3.3V 和 GND，模拟电压 0～3.3V，直接通过 `analogRead(1)` 读取（12 位 ADC 分辨率）。

### 3. 拨动开关（预设选择 ×4）

| GPIO | 网络名称 | 功能 | 软件配置 |
|---|---|---|---|
| GPIO11 | SW1 | 拨动开关 bit0 (值=1) | 输入模式，**​使能内部上拉​**（开关闭合接 GND） |
| GPIO12 | SW2 | 拨动开关 bit1 (值=2) | 输入模式，使能内部上拉 |
| GPIO13 | SW3 | 拨动开关 bit2 (值=4) | 输入模式，使能内部上拉 |
| GPIO14 | SW4 | 拨动开关 bit3 (值=8) | 输入模式，使能内部上拉 |

**​读取示例：​**

```cpp
uint8_t sw = 0;
if (!digitalRead(11)) sw |= 1;
if (!digitalRead(12)) sw |= 2;
if (!digitalRead(13)) sw |= 4;
if (!digitalRead(14)) sw |= 8;
// sw 范围 0~15，对应 16 种预设
```

### 4. 按钮（加载预设）

```c
按钮 → GPIO10
```

- 输入模式，**​使能内部上拉​**，按下为低电平。
- 配置为下降沿中断 (`FALLING`)，在中断服务中读取四个开关状态并加载对应预设。
- 软件消抖 200ms。

## 二、DAC 模块 PCM5102（纯硬件）

- **​无需任何软件控制​**，上电即工作。
- 使用的硬件接线：

`VCC` → 3.3V，`GND` → GND

`FLT`/`DMP`/`FMT` 已接地，`XMT`（静音）已接 3.3V（不静音）。

- 编程时只需关注 I²S 接口的 GPIO4/5/6。

## 三、耳机放大器 LM4881

- 纯硬件，**不需要代码控制**。
